<Project Sdk="Microsoft.NET.Sdk.Web">

  <ItemGroup>
    <ProjectReference Include="..\LpmSim.Core\LpmSim.Core.csproj" />
    <ProjectReference Include="..\LpmSim.Data\LpmSim.Data.csproj" />
  </ItemGroup>

  <ItemGroup>
    <PackageReference Include="ClosedXML" Version="0.104.2" />
    <PackageReference Include="Microsoft.AspNetCore.Authentication.Negotiate" Version="9.0.3" />
    <PackageReference Include="Microsoft.Identity.Web" Version="4.8.0" />
    <PackageReference Include="Microsoft.Identity.Web.UI" Version="4.8.0" />
    <PackageReference Include="MudBlazor" Version="7.15.0" />
  </ItemGroup>

  <PropertyGroup>
    <TargetFramework>net9.0</TargetFramework>
    <Nullable>enable</Nullable>
    <ImplicitUsings>enable</ImplicitUsings>
    <UserSecretsId>d895eeb3-6c53-45d7-99d2-1c4f7ead54ee</UserSecretsId>
    <!-- App version surfaced in the sidebar footer. Bump on each release;
         AssemblyInformationalVersion is what Assembly.GetCustomAttribute
         reads at runtime. See CHANGELOG.md at the repo root for the
         per-version release notes. -->
    <Version>1.14.80</Version>
    <AssemblyVersion>1.14.80.0</AssemblyVersion>
    <FileVersion>1.14.80.0</FileVersion>
    <InformationalVersion>1.14.80 (SIM Boxes report — two new columns: (1) Cont No — container number from whboxitems/WHBoxItemsExport.ContNo, pallet-level (matched on BoxNo + PalletNo); (2) Recent Batch — most recent Approved SIM batch (other than the current one) in the same country that contained this BoxNo. Surfaces "this box already shipped, why is it back?" reuse cases. Both columns appear in the Reports SIM Boxes tab, the Generate-preview Boxes tab, and both Excel exports.)</InformationalVersion>
  </PropertyGroup>

</Project>
