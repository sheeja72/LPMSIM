<Project Sdk="Microsoft.NET.Sdk.Web">

  <ItemGroup>
    <ProjectReference Include="..\LpmSim.Core\LpmSim.Core.csproj" />
    <ProjectReference Include="..\LpmSim.Data\LpmSim.Data.csproj" />
  </ItemGroup>

  <ItemGroup>
    <PackageReference Include="ClosedXML" Version="0.104.2" />
    <PackageReference Include="Microsoft.AspNetCore.Authentication.Negotiate" Version="9.0.3" />
    <PackageReference Include="Microsoft.Identity.Web" Version="4.8.0" />
    <PackageReference Include="Microsoft.Identity.Web.UI" Version="4.8.0" />
    <PackageReference Include="MudBlazor" Version="7.15.0" />
  </ItemGroup>

  <PropertyGroup>
    <TargetFramework>net9.0</TargetFramework>
    <Nullable>enable</Nullable>
    <ImplicitUsings>enable</ImplicitUsings>
    <UserSecretsId>d895eeb3-6c53-45d7-99d2-1c4f7ead54ee</UserSecretsId>
    <!-- App version surfaced in the sidebar footer. Bump on each release;
         AssemblyInformationalVersion is what Assembly.GetCustomAttribute
         reads at runtime. See CHANGELOG.md at the repo root for the
         per-version release notes. -->
    <Version>1.14.85</Version>
    <AssemblyVersion>1.14.85.0</AssemblyVersion>
    <FileVersion>1.14.85.0</FileVersion>
    <InformationalVersion>1.14.85 hotfix (WH SKU Investigation load failed with "Error converting data type varchar to bigint" — the 1.14.82 SUM(CAST(ISNULL(w.Slashed,0) AS bigint)) blew up because whboxitems.Slashed is varchar, and any non-numeric value made ISNULL fail. Switched both new aggregates to TRY_CAST: SUM(ISNULL(TRY_CAST(Slashed AS bigint),0)) + MAX(TRY_CAST(HOPrice AS decimal(18,2))) — non-numeric values fall through as NULL/0, and HOPrice now sorts numerically instead of lexicographically.)</InformationalVersion>
  </PropertyGroup>

</Project>
