<Project Sdk="Microsoft.NET.Sdk.Web">

  <ItemGroup>
    <ProjectReference Include="..\LpmSim.Core\LpmSim.Core.csproj" />
    <ProjectReference Include="..\LpmSim.Data\LpmSim.Data.csproj" />
  </ItemGroup>

  <ItemGroup>
    <PackageReference Include="ClosedXML" Version="0.104.2" />
    <PackageReference Include="Microsoft.AspNetCore.Authentication.Negotiate" Version="9.0.3" />
    <PackageReference Include="Microsoft.Identity.Web" Version="4.8.0" />
    <PackageReference Include="Microsoft.Identity.Web.UI" Version="4.8.0" />
    <PackageReference Include="MudBlazor" Version="7.15.0" />
  </ItemGroup>

  <PropertyGroup>
    <TargetFramework>net9.0</TargetFramework>
    <Nullable>enable</Nullable>
    <ImplicitUsings>enable</ImplicitUsings>
    <UserSecretsId>d895eeb3-6c53-45d7-99d2-1c4f7ead54ee</UserSecretsId>
    <!-- App version surfaced in the sidebar footer. Bump on each release;
         AssemblyInformationalVersion is what Assembly.GetCustomAttribute
         reads at runtime. See CHANGELOG.md at the repo root for the
         per-version release notes. -->
    <Version>1.14.93</Version>
    <AssemblyVersion>1.14.93.0</AssemblyVersion>
    <FileVersion>1.14.93.0</FileVersion>
    <InformationalVersion>1.14.93 (SIM Generate — new "Gap by UPC" tab on the Result preview, next to the existing per-box "Allocation Gap" tab. Per-item rollup of LPMSIM_AllocTrace: each row shows EligibleWhQty (qty in eligible whboxitems that entered the SIM box pool), SIM Qty (actually placed), Remaining Qty (the gap), and Top Reason (most-frequent SKIP_* Decision in the trace for that item). Filters by itemcode/name/division contains-match, Top Reason exact match, and Min Remaining. Includes an Excel export with totals.)</InformationalVersion>
  </PropertyGroup>

</Project>
