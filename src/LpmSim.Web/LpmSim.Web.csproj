<Project Sdk="Microsoft.NET.Sdk.Web">

  <ItemGroup>
    <ProjectReference Include="..\LpmSim.Core\LpmSim.Core.csproj" />
    <ProjectReference Include="..\LpmSim.Data\LpmSim.Data.csproj" />
  </ItemGroup>

  <ItemGroup>
    <PackageReference Include="ClosedXML" Version="0.104.2" />
    <PackageReference Include="Microsoft.AspNetCore.Authentication.Negotiate" Version="9.0.3" />
    <PackageReference Include="Microsoft.Identity.Web" Version="4.8.0" />
    <PackageReference Include="Microsoft.Identity.Web.UI" Version="4.8.0" />
    <PackageReference Include="MudBlazor" Version="7.15.0" />
  </ItemGroup>

  <PropertyGroup>
    <TargetFramework>net9.0</TargetFramework>
    <Nullable>enable</Nullable>
    <ImplicitUsings>enable</ImplicitUsings>
    <UserSecretsId>d895eeb3-6c53-45d7-99d2-1c4f7ead54ee</UserSecretsId>
    <!-- App version surfaced in the sidebar footer. Bump on each release;
         AssemblyInformationalVersion is what Assembly.GetCustomAttribute
         reads at runtime. See CHANGELOG.md at the repo root for the
         per-version release notes. -->
    <Version>1.14.83</Version>
    <AssemblyVersion>1.14.83.0</AssemblyVersion>
    <FileVersion>1.14.83.0</FileVersion>
    <InformationalVersion>1.14.83 (SKU Max "Built by" bug — the panel showed the prior builder's name (e.g. sheeja) alongside the new build's timestamp because the apply-phase UPDATE statement bumped CreateTS but never updated CreatedBy. Fix: display now reads BuiltBy from the per-period LPM_SimItemSkuMaxBuild header (which is MERGE-updated correctly on every build) instead of the per-row LPM_SimItemSkuMax.CreatedBy; the apply-phase UPDATE also now writes CreatedBy = @user so the per-row audit trail stops drifting.)</InformationalVersion>
  </PropertyGroup>

</Project>
