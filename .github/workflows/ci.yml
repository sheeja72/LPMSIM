name: CI

# Build the solution on every push to main and on every pull request.
# Catches broken commits before they get merged. No deploy here — that's
# in a separate workflow when you're ready.

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  build:
    name: Build & verify
    runs-on: windows-latest      # MudBlazor + .NET 9 build clean on Windows
    steps:
      - uses: actions/checkout@v4

      - name: Setup .NET 9
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: '9.0.x'

      - name: Restore
        run: dotnet restore

      - name: Build
        run: dotnet build --configuration Release --no-restore

      # When you add unit tests later, uncomment:
      # - name: Test
      #   run: dotnet test --configuration Release --no-build --verbosity normal
